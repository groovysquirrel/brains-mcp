*** Goals for the week *** 

We have a demo to show the power of the BRAIN on Wednesday morning. 
# Use cases
## The front end - need to demonstrate we can have a conversation
## Need to show a conversation interacting with a tool
## Need to show a novel, complex, self-adaptive example

The frontend is ready for work and needed for the demo.
# Frontend work
## Terminal
### Need to build a message router that will route terminal responses, content, status updates, etc. Route content renderes to the right components. 
### Similar to display mode, have a flag to turn on / off thoughts, concerns, questions... 
### Currently a bug where conversations don't continue; need front end testing
### Build a component to pick a conversation
## BRAIN Settings
### Build a config page for the brain, and setup config into DynamoDb. 
## MCP Server
### Need mcp server to show a list of the tools with a tester
## LLM Studio
### Build a component to test prompts; the LLM studio
## Money Manager
### Show what we are capturing in the money manager
## UI
### Update the layout to have a left bar with a list of tools (collapsable toolbar with a list of "My Brains", below a "My Cortex")

Some backend work but low priority as long as it works.
# Backend work: 
## Review SQS messages and  - filter by LLM, user, conversation, connection id, caller, etc. as message attributes. 
## Currently a bug where conversations don't continue; need front end testing


*** Instructions for kickoff ***

@04-21-2025.md @04-18-2025.md 

Review these readme files. 

The backend controller module is here
@brain-controller and the handlers are here @brain-controller 

The frontend is @src and we're going to start working with the terminal @terminal implementation.

I want to get started by building a message router that will route terminal responses, content, status updates, etc. Route content renderes to the right components. We already do this a little, but I want to formalize and standardize how this works to make it easier to maintain and follow. Currently we send updates to the larger component to update the button in the top right (disconnected, connecting, connected) while we also send messages to the status component and the terminal. Eventually we'll also want to send content to additional components. 

Do you have any questions before we get started? NOTE: Do NOT write code until we talk about what you'll do. 